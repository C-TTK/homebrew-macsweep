# MacSweep Homebrew Tap

Install the MacSweep Pro CLI:

```bash
brew tap C-TTK/macsweep
brew install macsweep
macsweep doctor
```

The formula installs the signed, notarized, pre-built universal binary from
`releases.macsweep.app`. It does not build from source and does not require
Xcode.
